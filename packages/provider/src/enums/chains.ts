export enum ChainTypes {
  EVM = 1,
  Solana = 2,
  Near = 3,
}
export enum SolanaChains {
  DevNet = 'devnet',
  TestNet = 'testnet',
  MainNet = 'mainnet',
}

export enum NearChains {
  TestNet = 'testnet',
  MainNet = 'mainnet',
}
