export enum ChainTypes {
  EVM = 1,
  Solana = 2,
  Near = 3,
}
export enum SolanaChains {
  devnet = 'devnet',
  testnet = 'testnet',
  mainnet = 'mainnet',
}

export enum NEAR_CHAINS {
  testnet = 'testnet',
  mainnet = 'mainnet',
}
